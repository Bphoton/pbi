// types only exits during development time

var myFun = () => 'response';
let a: number;
let b: boolean;
let c: string;

let d: number[]; d.push[1]
let e: boolean[];
let f: string[];
let g: [number, boolean, string]; //all three types would have to be in the array to be valid ts

let ff

